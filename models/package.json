{
	"name": "models",
	"version": "2.4.0",
	"main": "index.js",
	"private": true,
	"scripts": {
		"build": "tsc -b",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"typescript": "^4.6.4"
	}
}
