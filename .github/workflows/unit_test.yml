name: Unit Tests
on: [push]

jobs:
  build:
    
    runs-on: ubuntu-latest
        
    steps:
      - uses: actions/checkout@v2
      - name: Add missing Config-Files.
        run: cd ./backend/db/
        run: echo "{" > config.json
        run: echo " \"host\":\"\"," >> config.json
        run: echo " \"user\":\"\"," >> config.json
        run: echo " \"password\":\"\"," >> config.json
        run: echo " \"database\":\"\"" >> config.json
        run: echo "}" >> config.json
      # - name: Use Node.js 14.x
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: '14.x'
      # - run: npm ci
      # - run: npm run build --if-present
      # - run: npm test
